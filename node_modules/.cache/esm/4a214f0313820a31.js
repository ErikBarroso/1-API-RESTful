let express,mongoose;_138‍.w("express",[["default",["express"],function(v){express=v}]]);_138‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);require = require ("esm")(module);
//import 'dotenv/config';


const app = express();




// forma de ler JSON // middlewares
app.use(
  express.urlencoded({
    extended: true
  })
)
app.use(express.json())

// rotas da API
const personRoutes = require('./routes/personRoutes')

app.use('/person', personRoutes)

// rota inical / endpoint
app.get('/', (req, res) =>{
  // mostrar req

  res.json({message: 'Oi Express!'})

})

//entregar uma porta
const DB_USER = process.env.DB_USER
const DB_PASSWORD = process.env.DB_PASSWORD

mongoose.connect(`mongodb+srv://${DB_USER}:${DB_PASSWORD}@teste1.r5yvrb1.mongodb.net/bancodaapi20?retryWrites=true&w=majority`)
.then(() => {
  console.log('Conectamos ao MoongoDB')
  app.listen(3000)
})
.catch((err) => _138‍.g.console.log(err))